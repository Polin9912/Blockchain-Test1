/**
 * Task 9: Bytes to hex
 *
 * function bytesToHex(bytes)
 *
 * Parameters: bytes (number[]): Array of integers in range 0-255.
 * Returns: (string) The hex encoding with "0x" prefix and lowercase hex digits,
 *   e.g. "0x00ff10" for [0, 255, 16]. For empty array return "0x".
 */
function bytesToHex(bytes) {
  // TODO: implement
  throw new Error("Not implemented");
}

module.exports = { bytesToHex };
