/**
 * Task 8: Min and max
 *
 * function minMax(a, b)
 *
 * Parameters: a (bigint), b (bigint).
 * Returns: (object) { min: bigint, max: bigint }. Do not use Math.min / Math.max
 *   (they don't support BigInt in all environments). Use comparisons only.
 *
 * Examples:
 *   minMax(5n, 10n)  -> { min: 5n, max: 10n }
 *   minMax(10n, 5n)  -> { min: 5n, max: 10n }
 *   minMax(10n, 10n) -> { min: 10n, max: 10n }
 */
function minMax(a, b) {
  // TODO: implement
  throw new Error("Not implemented");
}

module.exports = { minMax };
