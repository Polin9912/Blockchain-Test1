/**
 * Task 4: Sort addresses
 *
 * function sortAddresses(addresses)
 *
 * Parameters: addresses (string[]): Array of Ethereum addresses, each "0x" followed by
 *   exactly 40 hexadecimal characters (e.g. "0x0000000000000000000000000000000000000001").
 * Returns: (string[]) A new array with the same addresses sorted in ascending order by the
 *   hex string (e.g. lexicographic comparison of the full string). Do not mutate the original array.
 *
 * Example:
 *   Input:  ["0x...03", "0x...01", "0x...02"]
 *   Output: ["0x...01", "0x...02", "0x...03"]
 */
function sortAddresses(addresses) {
  // TODO: implement
  throw new Error("Not implemented");
}

module.exports = { sortAddresses };
